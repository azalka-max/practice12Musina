<Page x:Class="Library.Pages.IssusePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Library.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="IssusePage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Оформление выдачи" FontSize="16" FontWeight="Bold" />

        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Книга:" VerticalAlignment="Center" Width="80" />
                <ComboBox Name="CBBooks" Width="420" DisplayMemberPath="Name" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Читатель:" VerticalAlignment="Center" Width="80" />
                <ComboBox Name="CBReaders" Width="300" DisplayMemberPath="FullName" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Дата возврата:" VerticalAlignment="Center" Width="80" />
                <DatePicker Name="DPReturnDate" />
            </StackPanel>

            <Button Content="Выдать"
                    x:Name="btnIssue"
                    Click="btnIssue_Click"
                    Width="120" />
            <Button Content="Назад"
                    x:Name="btnExit"
                    Click="btnExit_Click"
                    Width="120"
                    Margin="0, 5, 0, 0"/>
        </StackPanel>
    </Grid>
</Page>
