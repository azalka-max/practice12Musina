<Page x:Class="Library.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Library.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions> 

        <StackPanel DockPanel.Dock="Left" Width="180" Background="#FFC0975F"  Grid.Column="0">
            <TextBlock Text="Навигация" 
                           Margin="10" 
                           Style="{StaticResource TbBold20}"
                           HorizontalAlignment="Center"/>
            
            <Button Name="BtnBooks" 
                        Content="Каталог книг" 
                        Click="BtnBooks_Click"
                        Style="{StaticResource btnMP}"/>
            <Button Name="BtnIssue" 
                        Content="Выдать книгу" 
                        Click="BtnIssue_Click" 
                        Style="{StaticResource btnMP}"/>
            <Button Name="BtnLog" 
                        Content="Журнал выдач" 
                        Click="BtnLog_Click"
                        Style="{StaticResource btnMP}"/>
        </StackPanel>
    </Grid>
</Page>
