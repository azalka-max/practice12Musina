<Page x:Class="Library.Pages.IssuseLogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Library.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="IssuseLogPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Журнал выдач" FontSize="16" FontWeight="Bold" />

        <ListView Name="LVIssues" Grid.Row="1" Margin="0,10,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Книга" Width="350" DisplayMemberBinding="{Binding Book.Name}" />
                    <GridViewColumn Header="Читатель" Width="220" DisplayMemberBinding="{Binding Reader.FullName}" />
                    <GridViewColumn Header="Дата выдачи" Width="140" DisplayMemberBinding="{Binding IssueDate}" />
                    <GridViewColumn Header="Срок" Width="140" DisplayMemberBinding="{Binding DueDate}" />
                    <GridViewColumn Header="Дата возврата" Width="140" DisplayMemberBinding="{Binding ReturnDate}" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,6,0,0">
            <Button Content="Обновить" 
                    x:Name="btnRefresh"
                    Click="btnRefresh_Click" />
            <Button Content="Отметить возврат" 
                    x:Name="btnReturn"
                    Click="btnReturn_Click" 
                    Margin="10,0,0,0" />
        </StackPanel>
    </Grid>
</Page>
