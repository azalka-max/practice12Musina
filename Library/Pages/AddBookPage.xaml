<Page x:Class="Library.Pages.AddBookPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Library.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddBookPage">

    <Grid Margin="10">
        <StackPanel Width="500" VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            <TextBlock Text="Название:" />
            <TextBox Name="TBTitle" />

            <TextBlock Text="Жанр:" Margin="0,6,0,0" />
            <TextBox Name="TBGenre" />

            <TextBlock Text="Год:" Margin="0,6,0,0" />
            <TextBox Name="TBYear" />

            <TextBlock Text="Авторы (выберите из списка):" Margin="0,6,0,0" />
            <ListBox Name="LbAuthors" SelectionMode="Multiple" Height="120" />

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Сохранить" 
                        x:Name="btnSave"
                        Click="btnSave_Click"/>
                
                <Button Content="Отмена" 
                        Margin="8,0,0,0"
                        x:Name="btnExit"
                        Click="btnExit_Click"/>

                <Button Content="Добавить автора"
                        Margin="8,0,0,0" 
                        x:Name="btnAdd"
                        Click="btnAdd_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
